from collections import Counter

a = ['b' , 'b' , 'b' , 'c' , 'a' , 'w' , 'a' , 'c']
c = Counter(a) # A function defined in the python modules , that returns the count of each string as a dictonary (but not a dictonary.)

print(c)